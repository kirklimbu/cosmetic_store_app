import { FileExtensionCheckPipe } from './file-extension-check.pipe';

describe('FileExtensionCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new FileExtensionCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
